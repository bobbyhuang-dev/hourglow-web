{
  "name": "hourglow-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check site.js && npm run build && wrangler deploy --dry-run",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run check && wrangler deploy"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "wrangler": "4.128.0"
  }
}
